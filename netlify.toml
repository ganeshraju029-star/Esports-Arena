[build]
  command = "npm run build"
  publish = ".next"

# Handle API routes - let Next.js plugin handle them
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "10"
  NEXT_TELEMETRY_DISABLED = "1"
